import { setupTestingRouter } from './setup-testing-router';

describe('setupTestingRouter (fixture)', () => {
  it('dummy: should be callable', () => {
    expect(typeof setupTestingRouter).toBe('function');
  });
});
